{
    "version": "$APP_VERSION",
    "tag": "alpha",
    "sources": {
        "builds": [
        {
            "name": "$BUILD_NAME",
            "number": "$BUILD_NUMBER",
            "repository_key": "$PROJECT_KEY-build-info",
            "include_dependencies": false
        }
        ]
    }
}
